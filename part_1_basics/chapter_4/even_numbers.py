
numbers = list(range(1,6))
print(numbers)
# printing number starting from 1, ending on 6 (print until 5)

even_numbers = list(range(2,11,2))
print(even_numbers)
# adding 2 to each

random_numbers = list(range(5,46,4))
print(random_numbers)
# the first number is which number to start on
# the second number is the last number (which isn't printed)
# the last number shows how many to add in between


